#include <stdio.h>
/**
 *main - Entry point
 *Description: 'print al alphabets in lowercase except q and e
 *Return: Always 0 (Success)
 */
int main(void)
{
int n = 97;
while (n <= 122)
{
putchar(n);
n++;
if (n == 71)
{
break;
}
}
return (0);
}
